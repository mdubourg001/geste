{
  "name": "geste-test",
  "version": "0.1.7",
  "description": "",
  "main": "dist/geste.js",
  "bin": {
    "geste": "./dist/geste.js"
  },
  "files": [
    "./dist/dom.js",
    "./dist/geste.js",
    "index.d.ts"
  ],
  "scripts": {
    "build:test-runner": "esbuild src/geste.ts --outfile=dist/geste.js --external:esbuild --loader:.ts=tsx --platform=node --target=node12 --format=cjs --banner:js=\"#!/usr/bin/env node\" --bundle",
    "build:dom": "esbuild src/dom.ts --outfile=dist/dom.js --loader:.ts=tsx --platform=node --target=node12 --format=cjs --bundle",
    "build": "pnpm build:test-runner && pnpm build:dom",
    "test": "pnpm build && cd examples/jest-replacement && pnpm geste",
    "prepublish": "pnpm build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "17.0.9",
    "np": "^7.6.0"
  },
  "dependencies": {
    "chalk": "^5.0.0",
    "esbuild": "0.14.11",
    "expect": "^27.4.6",
    "glob": "^7.2.0",
    "linkedom": "^0.13.0",
    "pkg-up": "4.0.0"
  },
  "peerDependencies": {
    "esbuild": "*"
  }
}
